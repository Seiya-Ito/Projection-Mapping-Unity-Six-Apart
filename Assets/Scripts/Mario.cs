using UnityEngine;
using System.Collections;

public class Mario : MonoBehaviour {

	// Use this for initialization
	void Start () {
		this.renderer.material.color = new Color(225, 215, 0);
	}
	
	// Update is called once per frame
	void Update () {
	
	}
}
